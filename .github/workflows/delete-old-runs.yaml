name: Delete old workflow runs

on:
  schedule:
    - cron: "5 4 * * 0"   # Todos los domingos a las 04:05 UTC
  workflow_dispatch:

jobs:
  cleanup:
    uses: Nivaes/Nivaes.Workflows/.github/workflows/delete-old-runs.yaml@develop  
